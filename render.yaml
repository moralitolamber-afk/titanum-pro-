services:
  - type: web
    name: titanium-pro
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run web_dashboard.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: STREAMLIT_SERVER_HEADLESS
        value: "true"
      - key: STREAMLIT_BROWSER_GATHER_USAGE_STATS
        value: "false"
